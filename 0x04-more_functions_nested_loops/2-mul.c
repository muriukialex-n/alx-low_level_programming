#include "main.h"
/**
 * mul - multiplies two integers
 * @x: first integer
 * @y: second integer
 * Return: Result of multiplication between x param and y param
 */
int mul(int x, int y)
{
	return (x * y);
}
